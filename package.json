{
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch",
    "dev": "live-server src"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "tailwindcss": "^4.0.0"
  }
}
